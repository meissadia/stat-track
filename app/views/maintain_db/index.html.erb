<main>
  <section id="buttons">
    <h1>Populate Database</h1>
    <div class=center>
      <p><%= link_to 'Update DB', '/maintain_db/updateDb', method: :post %></p>
    </div>
  </section>
  <section id=status>
    <% @status.each do |msg| %>
      <% if !msg.empty? %>
        <p><%= msg %></p>
      <% end %>
    <% end %>
  </section>
</main>
